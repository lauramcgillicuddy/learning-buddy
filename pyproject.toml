[project]
name = "learning-buddy"
version = "0.1.0"
description = "A Reachy Mini learning companion with voice, RAG, and quiz modes"
requires-python = ">=3.11"

[project.scripts]
buddy = "main:app"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
